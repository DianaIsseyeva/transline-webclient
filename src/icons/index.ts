import { activeApp } from './activeApp';
import { archApp } from './archApp';
import { car } from './car';
import { carrier } from './carrier';
import { client } from './client';
import { mgrs } from './mgrs';
import { refs } from './refs';
import { transport } from './transport';

export const icon = {
  car,
  activeApp,
  archApp,
  carrier,
  client,
  mgrs,
  refs,
  transport,
};
